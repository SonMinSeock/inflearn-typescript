const user: {
    id: number;
    name: string;
} = {
    id: 1,
    name: "손민석",
};

const user2: {
    id?: number;
    name: string;
} = {
    name: "아이유"
}

const config: {
    readonly apiKey: string;
} = {
    apiKey: "My API Key",
};

// config.apiKey = "23"